import { limitBreaks } from "./limitBreaks.js";
import { mythicFeats } from "./mythicFeats.js";
import { mythicReroll } from "./mythicReroll.js";

export let mythic = {
    'blessingFeats': mythicFeats,
    'reroll': mythicReroll,
    'limitBreak': limitBreaks
}